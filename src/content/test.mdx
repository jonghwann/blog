# 테스트 페이지

안녕하세요! 이것은 **MDX** 테스트 페이지입니다.

## 코드 하이라이팅 테스트

```typescript showLineNumbers {1,3-5}
const greeting = 'Hello, World!';
console.log(greeting);

function add(a: number, b: number): number {
  return a + b;
}
```